# Raw Data Placeholder

Place raw ToolBench `G1_answer` JSON files in `data/raw/toolbench/G1_answer/` if you want to rebuild the public dataset from source JSON files.

The released CSV datasets in `data/datasets/` are already prepared and can be used directly to reproduce the reported experiments.
