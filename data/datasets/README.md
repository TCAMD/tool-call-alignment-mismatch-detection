# Dataset Files

Large dataset CSV files are distributed through the GitHub Release asset `paper-data-v1.0.zip`.

After downloading the asset, unzip it at the repository root. The following paths will then be created:

- `data/datasets/toolbench_mismatch_dataset.csv`
- `data/datasets/ratio_samples/*.csv`

The notebooks are already configured to read these repository-relative paths.
